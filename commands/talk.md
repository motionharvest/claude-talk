---
description: Speak the last response aloud
allowed-tools: Bash(~/.claude/talk.sh:*)
---

!`~/.claude/talk.sh $ARGUMENTS`

The command above already ran and its output is shown. Reply with nothing but
that one line of output (or its error). Do not add commentary.
